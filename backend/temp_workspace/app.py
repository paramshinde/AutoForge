from fastapi import FastAPI, Request, Responseimport uvicornfrom pydantic import BaseModelclass User(BaseModel):username: stremail: strempassword: strapp = FastAPI()@app.post('/register')async def register(user: User):try:# Save user to database# For demonstration purposes, we'll just print the user dataprint(user)response = {'success': True}except Exception as e:print(e)response = {'success': False}return responseif __name__ == '__main__':uvicorn.run(app, host='0.0.0.0', port=8000)